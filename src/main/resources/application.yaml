spring:
  application:
    config:
      import: "dotenv:./.env"
    name: pixel-battle
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}


management:
  endpoints:
    web:
      exposure:
        include: health

jwt:
  secret: ${JWT_SECRET}
